#include "LegalMoveFinder.h"

namespace shoryu::core
{
	std::vector<Position> findLegalMoves(const Board& board, Position from)
	{
		std::vector<Position> legalMoves;
		// ここに合法手を見つけるロジックを実装します。
		// 例えば、駒の種類や位置に基づいて移動可能な位置を計算します。
		return legalMoves;
	}
}
